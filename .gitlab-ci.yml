stages:
  - testing
  - publishing

.testing_template: &python_matrix
  stage: testing
  script:
    - make install-development
    - make coverage

code quality:python_v3.6:
  <<: *python_matrix
  image: python:3.6-buster

code quality:python_v3.7:
  <<: *python_matrix
  image: python:3.7-buster

code quality:python_v3.8:
  <<: *python_matrix
  image: python:3.8-buster
  artifacts:
    paths:
      - htmlcov
    expire_in: 3 days

pages:
  image: debian:latest
  stage: publishing
  dependencies:
    - code quality:python_v3.8
  script:
    - rm --force --recursive public
    - cp --recursive htmlcov public
  artifacts:
    paths:
      - public
    expire_in: 1 week
  only:
    - master
    - schedules
    - tags
